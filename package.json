{
  "name": "ha-nova",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --enable-source-maps --watch src/runtime/main.ts",
    "seed:llat": "node scripts/dev-seed-ha-llat.mjs",
    "smoke:addon:build": "bash scripts/smoke/addon-local-build.sh",
    "smoke:addon:run": "bash scripts/smoke/addon-local-run.sh",
    "smoke:addon:http": "bash scripts/smoke/addon-http-smoke.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "home-assistant-js-websocket": "^9.6.0",
    "ws": "^8.19.0",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
