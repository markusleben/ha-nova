# Release body draft — next release

Status: `active`
Version number is provisional: the masterplan ships ONE release at the end of all waves (see `docs/work/masterplan-2026.md`). The final number is fixed in the release-prep PR; this draft collects the user-facing claims until then.

## Release-bound README edits (do NOT land before the release-prep PR)

- `README.md:159` — "19 task skills plus the HA NOVA context skill" → the count at release time (20 after `diagnose`; more with the coverage waves).
- Platform claim (wave 4): the relay also runs as a standalone Docker container, so Home Assistant Container/Core installs are supported — the add-on stops being a hard requirement.
- Link the comparison page (wave 5) from "How it works".

## New Features (draft)

- **Guided uninstall.** `ha-nova uninstall` now walks you through the Home Assistant side too: removing the NOVA Relay app, removing the repository, and revoking the "NOVA" access token. The cleanup checklist is always shown — even when Home Assistant is unreachable.
- **Update notices in every AI client.** Codex, OpenCode, Antigravity, and Hermes now surface HA NOVA updates during normal skill use; previously only Claude did.
- **New skill: `ha-nova:diagnose`.** Root-cause a concrete failure — "why didn't my morning automation run?" — from run traces, the error log, the system log, and bounded history windows, with an optional (and always reset) debug-level escalation.

## What To Watch (draft)

- Uninstall always prints the Home Assistant cleanup checklist now, including the access-token revocation step that was previously never mentioned.
- Set `HA_NOVA_NO_UPDATE_NUDGE=1` to silence the new update notices.

## Internal (not release notes)

Workstream A hardening: skill template v2 + structural linter, bootstrap-independent Safety Core in every skill (portable safety gates for non-Claude agents), token diet on the write path, Agent Skills standard frontmatter.
