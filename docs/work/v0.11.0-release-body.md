# v0.11.0 Release Body (draft)

Status: active — collects unreleased feature claims until the release-prep PR.

## New Features

- **New skill: `ha-nova:energy`** — analyze your energy data conversationally (per-device consumption and costs, solar/battery/grid KPIs, period comparisons, standby hunting) and safely edit Energy dashboard sources and tracked devices with preview, confirmation, and validate-backed verification.
- **New skill: `ha-nova:maintenance`** — repair recorder statistics (orphaned statistics, unit mismatches, reversible sum-spike fixes), purge recorder history, and clean up dead entity-registry entries — grouped previews, typed-token confirmation for everything destructive, per-item verification.

## Release-prep reminders

- README: bump skill inventory 17 → 19 task skills, add the `energy` and `maintenance` table rows.
